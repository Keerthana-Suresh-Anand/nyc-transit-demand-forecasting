name: Pipeline — Prediction

on:
  workflow_dispatch: {}

env:
  AWS_ACCESS_KEY: ${{ secrets.AWS_ACCESS_KEY }}
  AWS_SECRET_KEY: ${{ secrets.AWS_SECRET_KEY }}
  AWS_BUCKET_NAME: ${{ secrets.AWS_BUCKET_NAME }}
  AWS_REGION: ${{ secrets.AWS_REGION }}
  MLFLOW_TRACKING_URI: sqlite:///mlflow.db

jobs:
  predict:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Install DVC
        run: pip install "dvc[s3]>=3.0"

      - name: Pull silver data via DVC
        run: |
          mkdir -p data/silver data/gold mlruns
          dvc remote modify --local s3remote url s3://$AWS_BUCKET_NAME/dvc-cache
          dvc pull data/silver/mta_weather_merged.parquet
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}

      - name: Download MLflow state from S3
        run: |
          aws s3 cp s3://$AWS_BUCKET_NAME/mlflow/mlflow.db mlflow.db
          aws s3 sync s3://$AWS_BUCKET_NAME/mlflow/mlruns/ mlruns/
          if [ -z "$(ls -A mlruns/)" ]; then
            echo "ERROR: mlruns/ is empty after S3 sync — run the training pipeline first"
            exit 1
          fi
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_KEY }}
          AWS_DEFAULT_REGION: ${{ secrets.AWS_REGION }}

      - name: Run prediction pipeline
        run: |
          IMAGE="${{ secrets.DOCKERHUB_USERNAME }}/nyc-transit-pipeline:stable"
          docker pull "$IMAGE"
          IMAGE_DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' "$IMAGE")
          echo "Running image: $IMAGE_DIGEST"
          docker run --rm \
            -v "$(pwd)/data:/app/data" \
            -v "$(pwd)/mlflow.db:/app/mlflow.db" \
            -v "$(pwd)/mlruns:/app/mlruns" \
            -e AWS_ACCESS_KEY="$AWS_ACCESS_KEY" \
            -e AWS_SECRET_KEY="$AWS_SECRET_KEY" \
            -e AWS_BUCKET_NAME="$AWS_BUCKET_NAME" \
            -e AWS_REGION="$AWS_REGION" \
            -e MLFLOW_TRACKING_URI="sqlite:///mlflow.db" \
            -e PIPELINE_IMAGE_DIGEST="$IMAGE_DIGEST" \
            "$IMAGE" \
            -m pipelines.run_prediction
